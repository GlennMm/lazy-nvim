local options = {
  ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "rust", "go", "typescript", "python" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
